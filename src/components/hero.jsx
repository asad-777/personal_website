'use client'

export default function Hero() {
  return (
    <section
      id="hero"
      className="relative w-full h-screen overflow-hidden bg-base-100 flex items-center justify-center"
    >
      {/* Hero content goes here */}
    </section>
  )
}
